<ul class="people">
  {{#each model}}
    {{#listed-person name=name}}
    {{/listed-person}}
  {{/each}}
</ul>

{{input class="name" value=name placeholder="Name"}}
<button {{action 'addPerson'}} class="submit">Add</button>